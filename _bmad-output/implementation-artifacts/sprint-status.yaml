---
project_name: pocpod0
sprint_generated: 2026-03-18
last_updated: 2026-07-31  # Story 8.2 code review complete (3-layer): 3 decisions closed by live verification against pod.nicolasdb.eu (AC6 both non-throw cases, AC4 concurrency w/ distinct-arg clients, AC8 forced-login-failure log hygiene) — all passed; 3 patches (parsePort validation, ALLOWED_HOSTS wiring, progress-report dedup); 6 deferred; 3 dismissed. Moved to done. # Story 8.1 code review complete (3-layer blind/edge/acceptance): 2 decisions resolved w/ Nicolas (revoke keeps scope='both' default; fresh-ACL creation now auto-injects acting session's own control:true, closing brief §6 pitfall-4 self-orphan risk), 6 patches applied (403 detection now status-code-first not bare substring match; onboarding.js OWNER_CLIENT_ID guard now runs before login attempt; grantAccess/setPublicAccess throw on invalid scope instead of silent no-op-but-report-success; whoami.js ACL error branch handles non-Error throws; File List corrected to include all 7 new toolkit files, not just podClient.js/wacManager.js), 2 deferred (getAgentAccess null-vs-no-access ambiguity; deleteResource silent no-op on containers, already flagged for a future story), 4 dismissed as noise. Moved to done. # Story 8.1 live-verified against pod.nicolasdb.eu with AGENT (nicolas_claude) client-credentials token (no OWNER creds used) — 2 real bugs found+fixed live (podClient.js writeFile string->Buffer coercion; wacManager.js grantAccess/setPublicAccess now normalize a Control-less 403 to the documented error instead of a raw stack). AC2's WAC-read-on-data-pod clause is a verified negative (CSS requires Control to read .acl, correctly denied). AC3 substituted setPublicAccess+anon fetch for a named second-agent grant (no 2nd WebID available). All throwaway test resources cleaned up, both pods confirmed restored. Moved to review. # Epic 8 added (sprint change proposal 2026-07-30): Solid MCP Connector (Quest B handoff), independent of Epic 4/7, toolkit unzipped into mcp-connector/, story 8.1 (WAC Turtle port from Story 7.3) drafted ready-for-dev. # Story 7.3 code review complete (3-layer, see story file Review Findings) — 1 decision resolved (ACL rewrite fail-loud guard ahead of team-pod group sharing), 8 patches applied (Turtle-injection webId validation, path-traversal filename sanitization, dead-code cleanup, delete-confirm descendant count wired in, ACL-fetch-error vs private distinguished, remove()/rename() no longer proceed past a swallowed listing failure, rename rollback on partial failure), 4 deferred (TOCTOU collision checks, upload batch hardening → 7.6, delegate-overwrites-owner edge case, Turtle lexer escaped-quote handling) — logged in deferred-work.md. Moved to done. # Story 7.2 implemented (restyle CSS identity pages via main.css+main.html.ejs bind-mount override, tokens.css SSOT, real email + required-passphrase fix, box->locker copy, consent dl grid fix). Committed 8e3fc9d, NOT pushed. DEPLOYED to VPS by Nicolas (manual) 2026-07-23, live-verified on pod.nicolasdb.eu (tokens.css 200, cache-bust versions correct, locker copy live, consent grid fix live). Code-reviewed 2026-07-23 (3-layer adversarial/edge-case/acceptance-auditor review, see story file Review Findings) — 5 patches applied (email regex dedup, registerAccount retry/email bug fixed, AC9 SSOT properly fixed via var() fallbacks + JS palette dedup, AC2 copy gap fixed via new create-pod.html.ejs/consent.html.ejs template overrides, stale doc note), 3 decisions resolved, 4 items deferred (unmasked password field, Google Fonts privacy leak on auth pages, :has() no-fallback, AC6 evidence gap) — logged in deferred-work.md. Fixes committed 66af50b and DEPLOYED to VPS by Nicolas (manual) 2026-07-23 — VPS now running the post-review state. Open backlog surfaced during 7.2: (1) ACL "Requests" tab is a hardcoded UI stub (index.html:555, fake seed, never persisted, no real request intake) — separate feature, not a 7.2 bug. (2) Story 7.1's resumability patches were never re-verified live on VPS. (3) 17 orphan CSS dev accounts on VPS still unswept, no HTTP delete path. # Story 7.1 code review done: made registerAccount resumable/idempotent (was orphaning CSS accounts on retry), + 5 patches (crypto password, empty-slug guard, API-shape guards, error detail). Found 17 orphan dev accounts on VPS (.internal/accounts) needing manual sweep — logged in deferred-work. NOTE: patches applied but not re-verified live on VPS. # Story 4.4.1 opened (unplanned VPS infra consolidation — CSS onto shared nginx-gateway, security fix, OpenClaw retirement kicked off but not finished). Epic 7 added (sprint change proposal 2026-07-21): Epic 4 parked, pod-owner backoffice prioritized. Stories 7.1 and 7.2 drafted, ready-for-dev. Story 7.1 implemented + deployed to VPS, moved to review — also fixed a live CSS auth-config bug (debug-auth-header.json was disabling all real OIDC logins, not just this story's new accounts).
tracking_system: file-system
project_key: NOKEY
communication_language: English

# Sprint Status Tracking
development_status:
  # Epic 1: Pod Sovereignty & Access Control
  epic-1: done
  story-1-1-project-scaffold-service-orchestration: done
  story-1-2-nginx-content-negotiation-spike: done
  story-1-3-pod-provisioning-acl-configuration: done
  story-1-4-acl-grant-revocation-audit: done
  story-1-5-troll-acl-enforcement-validation: done
  epic-1-retrospective: done

  # Epic 2: Semantic Data Intelligence
  epic-2: done
  story-2-1-oslo-vocabulary-schema-contract: done
  story-2-2-xapi-oslo-rdf-ingestion-pipeline: done
  story-2-3-oxigraph-setup-rdf-storage-with-provenance: done
  story-2-4-round-trip-xapi-recovery-verification: done
  story-2-5-qdrant-setup-vector-embeddings: done
  story-2-6-bidirectional-traceability-embedding-triple-pod: done
  story-2-7-troll-sparql-injection-validation: done
  story-2-8-troll-vector-privacy-validation: done
  epic-2-retrospective: done

  # Epic 3: Cross-Context Learning Insights
  epic-3: done
  story-3-1-shared-sparql-skill-foundation: done
  story-3-2-shared-qdrant-skill-foundation: done
  story-3-3-openClaw-agent-infrastructure: done
  story-3-4-claire-cross-context-insight-discovery: done
  story-3-5-fatima-unified-parental-view: done
  story-3-6-isabelle-evidence-based-policy: done
  story-3-7-graph-only-vs-hybrid-comparison: done
  story-3-8-troll-cross-inference-validation: done
  # NOTE (2026-03-25): Story 3.8 enables OpenClaw chatCompletions API — prerequisite for Epic 5/6 agent interaction
  # NOTE (2026-03-25): Story 3.7.1 (side-quest) Pipeline Dashboard TUI — seed for Epic 6 mission control
  epic-3-retrospective: done

  # Epic 5: Data Sovereignty Lifecycle (RESEQUENCED: was after Epic 4, now NEXT)
  # NOTE (2026-03-25): Epic 3 retro decision — resequence to 5→6→4
  # Rationale: acl-manage skill is prerequisite for both Marc (transfer) and Ayoub (governance).
  # Anagnorisis narrative enriches consent mechanics (consent-as-architecture, access receipts,
  # tombstone revocation, civic signal). Build consent machinery first.
  # Epic 5 stories need review against consent-as-architecture before implementation.
  epic-5: done
  story-5-1-ayoub-governance-transition: done
  story-5-2-deletion-cascade-propagation: done
  story-5-3-troll-deletion-timing-validation: done
  # Anagnorisis backlog stories (added 2026-03-25 — consent-as-architecture principles)
  story-5-4-access-receipt-written-to-pod: done
  story-5-5-consent-grant-as-rdf-resource: done
  story-5-6-ephemeral-time-scoped-consent: done
  epic-5-retrospective: done
  # NOTE (2026-03-30): Retro confirmed — all 6 stories shipped incl. 3 Anagnorisis backlog stories.
  # LEGACY-PRIV1: pre-5.2 Qdrant points carry pod_resource_uri — handled by compose down -v reset (no migration needed for PoC).
  # camp-dietary-aggregate.ttl not wired into pipeline — Story 6.0 added to fix.
  # Demo reset strategy: compose down -v → up -d → run_pipeline.py (single command after Story 6.0).

  # Epic 6: Adversarial Trust Report & Mission Control (RESEQUENCED: was last, now before Epic 4)
  # NOTE (2026-03-25): Dashboard before demo — project lead navigating blind = Isabelle's pain.
  # TUI pivot confirmed: Rich TUI + JSONL (Story 3.7.1 seed), NOT FastAPI+HTMX.
  # Two-terminal demo model: pipeline/troll in one, dashboard in the other.
  # NOTE (2026-03-30): Story 6.0 completed — pipeline provision as Stage 0.
  # NOTE (2026-03-31): Pipeline completed successfully! Total: 3320.6s

  epic-6: done
  story-6-0-pipeline-provision-stage-zero: done
  story-6-1-troll-comprehensive-run-and-report: done
  story-6-2-mission-control-dashboard-implementation: superseded  # TUI approach abandoned — see 6-2-acl-dashboard.md
  story-6-2-acl-dashboard: done  # PIVOT 2026-04-01: FastAPI + HTML, queries CSS ACLs directly. Code review 2026-04-02: approved with 4 minor test fixes applied.
  story-6-3-funder-intervention-points: done  # Code review 2026-04-04: approved, 8 patches applied
  epic-6-retrospective: done  # 2026-04-12: Retro completed. Decisions: Discord over Telegram, seed-on-boot for agent workspaces, troll on Discord (#security_logs), ACL dashboard keeper. Five carried items must land as Story 4.1 prerequisites.

  # Implementation Gaps (documented in epic-6-retro-2026-04-12.md, section "Technical Debt Register")
  # AC2-GAP: Troll category launch env vars not validated (low — troll migrating to Discord)
  # IG-1: CSS 404 semantics undocumented (low — attach to Story 4.1 prereq)
  # CSS_CONNECT_URL vs CSS_BASE_URL: both default :3000, document if they diverge

  # Epic 4: Student Transfer & Data Portability (RESEQUENCED: was next, now CAPSTONE)
  # NOTE (2026-03-25): Marc's transfer becomes the integration test for the full stack.
  # Built WITH dashboard running — first epic where project lead watches live.
  # Consent mechanics (Epic 5) + dashboard (Epic 6) both available.
  # NOTE (2026-04-12): Epic 6 retro decisions shape Epic 4 infrastructure:
  #   - Discord as interaction layer (all agents bound to channels)
  #   - Seed-on-boot agent workspaces (evolvable, factory-resettable)
  #   - Troll on Discord (#security_logs heartbeat)
  #   - ACL dashboard keeper (runs alongside)
  #   - Nicolas as new user (own pod + agent proves onboarding)
  #   - Five carried items (NFR-LAG, IG-1, ACL-DRIFT, consent catalog, invalidated assumptions) must land as Story 4.1 prerequisites
  epic-4: in-progress
  story-4-0-discord-seed-on-boot-infrastructure: done
  # Side quest split (2026-04-14): WebUI unreachable under podman compose — root cause identified
  # as podman-compose silently using bridge instead of pasta (podman-compose#967, podman#24285).
  # 4-0-1 is timeboxed local fix attempt; 4-0-2 is VPS deploy (strategic fallback, also unblocks pilot-bridge).
  story-4-0-1-force-pasta-local-fix: done  # 2026-04-15: WebUI+pairing fixed; podman 5.8.1 + openclaw v2026.4.14
  story-4-0-2-vps-deploy-hetzner: done
  # UNPLANNED (2026-07-21): "review last couple months" session surfaced pocpod0 wasn't
  # using the VPS's shared nginx-gateway convention. See 4-4-1-nginx-openclaw-retirement-css-hardening.md.
  story-4-4-1-nginx-openclaw-retirement-css-hardening: in-progress  # CSS repointed onto pod.nicolasdb.eu via nginx-gateway + security fix done; OpenClaw retirement, tunnel/portainer migration, data recreate, pod-lifecycle deep-dive still open
  story-4-1-marc-school-transfer-via-discord: ready-for-dev     # was: story-4-1-marc-seamless-student-transfer
  story-4-2-cross-community-data-handling: backlog
  story-4-3-nicolas-onboarding-proof: backlog
  story-4-4-troll-heartbeat-security-monitor: backlog
  story-4-5-ayoub-revokes-isabelle-reacts: backlog
  epic-4-retrospective: optional

  # Epic 7: Pod Owner Experience (ADDED 2026-07-21 — sprint change proposal 2026-07-21)
  # Epic 4 PARKED at this date: 4.1 stays ready-for-dev, resume after Epic 7.
  # Origin: runnable mockup bundle "SOLID Pod Management Interface.zip" (see backoffice/ + its HANDOFF.md).
  # Deploy target: https://pod.nicolasdb.eu/ root (replaces CSS default welcome page), not a separate hostname.
  epic-7: in-progress
  story-7-1-backoffice-deploy-real-account-registration: done
  story-7-2-css-registration-pages-restyle: done  # Code review 2026-07-23 (3-layer: blind/edge/acceptance): 3 decisions resolved (AC7/8 demo-mode gate accepted as-is, AC9 SSOT fixed via CSS var() fallbacks + JS dedup, AC2 copy gap fixed via create-pod.html.ejs + consent.html.ejs overrides), 5 patches applied (email regex dedup, registerAccount retry/email bug, stale scope note, AC9, AC2), 4 items deferred. Live-reverified: full account->password->pod-create round trip 200/200/200 through new templates.
  story-7-3-my-things-crud-acl-fix: done  # 2026-07-23: drafted from live CSS ACL audit. Core = fix client-side public-RW ACL bug (CSS honors public RW, backoffice writes ineffective .acl — missing acl:default on containers); + file-upload UI (overwriteFile already supports Blob/any-type, only picker missing); CRUD completeness; ACL inheritance truth-telling; root-Control self-lockout guard. IMPLEMENTED 2026-07-23: ACL write bug fixed (direct .acl Turtle read/write replacing buggy universalAccess calls), live-verified against real VPS pod via a zero-dependency Node DPoP session (no browser available this session) — repro'd the bug (anon PUT to child of "public RW" container = 401 pre-fix), confirmed fix (205 post-fix, byte-identical re-GET), confirmed revoke round-trip and additive grants, confirmed CRUD + PNG upload byte-identical. Root-Control lockout closed structurally (owner grant never editable via this UI, not just warned). NOT yet deployed to VPS; NOT re-tested through actual browser UI — code + live-API verification only, moved to review for code-review + eventual browser/VPS check. CODE REVIEW 2026-07-23 (3-layer blind/edge/acceptance, in progress): decision D1 resolved w/ Nicolas — `_writeAcl`'s full-document rewrite silently dropped any Turtle authorization it can't parse (acl:agentGroup/acl:origin/unrecognized agentClass), a real risk now that team pods (group-based sharing) are imminent. Fixed cheap/safe for now: `getAccess` flags unparseable blocks as `unknownBlocks`; `setAgentAccess`/`setPublicAccess` refuse to write (throw) rather than silently deleting them; `rename`'s ACL carry-over skips + warns instead of silent loss. Real round-trip/group support deferred until team-pod requirements are concrete.
  story-7-4-apps-and-credentials: done  # 2026-07-23: code review complete — 9 patches applied (rename-rollback data-loss fix, ACL carry-over fix, AC8 focus-visible/aria-modal/Escape, double-submit guards, response validation, 404-on-revoke, dead-code removal), 8 deferred, 0 dismissed-as-decision. Still needs browser/VPS pass before real-world sign-off. IMPLEMENTED. Client-creds mint/list/revoke UI in People&apps. KEY FINDING: OIDC/WebID login ≠ CSS-Account-Token — credentials API needs separate email+password sign-in (controls.account.clientCredentials only on authed /.account/ index, GET must omit content-type); UI has explicit "Unlock app management" gate w/ honest explanation. RealBackend account-session+cred methods + DemoBackend stubs. One-time secret reveal (gated tap, secret-free copyable recipe, never persisted/logged, cleared on close). Inbound/outbound People split + WAC acts-as-me note. Full mint→DPoP-token→authed-GET(200)→revoke(200)→token-401-invalid_client round-trip LIVE-CONFIRMED on v0.5 throwaway. Code + live-API verified only; NO browser/VPS pass yet (no browser this session, same as 7.3). pod-api cache-buster 7-4-1.  # drafted. Client-credentials mint/list/revoke all live-confirmed incl. HTTP revoke (DELETE resourceUrl -> 200); one-time-secret UX; connection recipe for Discord/Matrix bridges; People-&-apps inbound/outbound split; honest WAC per-app-limitation framing.
  story-7-5-export-backup-portability: ready-for-dev  # 2026-07-23: drafted. Owner-facing pod export via LDP crawl -> downloadable archive + access-metadata sidecars + honest portability explainer (no standard pod archive; WebID-rebind breaks .acl on cross-provider move). Import/restore deferred to follow-on. Distinct from operator-level make vps-backup.
  story-7-6-file-manager-hardening: ready-for-dev  # 2026-07-23: drafted from 7.3 live-testing findings. Hardening pass on top of 7.3 CRUD/ACL spine: cross-folder MOVE (generalize rename), bulk multi-select delete/move, transfer progress for many/large files, large-folder listing robustness, graceful partial-failure (copy-verify-delete ordering, no data loss). solid-file-manager/solid-file-client as REFERENCE not dependency (decision w/ Nicolas 2026-07-23: it lacks our ACL UI + inline edit + two-tap delete; adopting = rebuild differentiators on foreign arch). Keep pinned Inrupt libs; reuse 7.3 DPoP verification harness. WCAG AA + no-regression.
  epic-7-retrospective: optional

  # Epic 8: Solid MCP Connector (ADDED 2026-07-30 — Quest B handoff, MISSION_BRIEF_solid-mcp-connector.md)
  # Independent of Epic 7/4 — new mcp-connector/ Node service, not a backoffice/pipeline change.
  # Story 8.1 must land before 8.5 (live verification) — ports Story 7.3's ACL Turtle fix into
  # wacManager.js instead of trusting the toolkit's untested Inrupt universal-access calls.
  epic-8: in-progress
  story-8-1-wac-hardening-verification: done
  story-8-2-http-transport: done  # 2026-07-31: code review complete (3-layer blind/edge/acceptance). All 3 decision-needed findings closed by LIVE verification against pod.nicolasdb.eu rather than by argument, all 3 passed: (a) AC6's two non-throw cases confirmed live — solid_get_permissions on hyperscope_ndb/shared/ (no Control) returns the documented Control message with isError:true, and on a non-RDF .txt returns the RDF-only message; (b) AC4 concurrency confirmed — 3 simultaneous clients issuing DISTINCT calls each got its own correct response (note: the first attempt used identical args, which cannot reveal interleaving — the distinct-arg run is the real evidence); (c) AC8 failure-path log hygiene confirmed — forced bad-secret login logs only "invalid_client", grep clean of secret/client-id/authorization/bearer/token. 3 patches applied: parsePort() rejects non-integer/out-of-range PORT (was NaN->app.listen), ALLOWED_HOSTS env wired into createMcpExpressApp() per Dev Notes advice the impl had skipped, progress-report duplicate 8.3 line deduped. 6 deferred to deferred-work.md (session expiry/re-auth, substring-based error classification, static /healthz, no body-size cap or timeout, getAgentAccess null-guard trap for 8.3, fatal-log stack-trace loss). 3 dismissed as noise. Post-patch re-verify: verify-http.js ALL CHECKS PASSED, healthz ok, GET/DELETE 405, logs clean. SANDBOX NOTE: env vars do NOT propagate into node here (same class of quirk as cwd) — parsePort was unit-checked directly instead. Moved to done. # 2026-07-31: implemented. stdio->StreamableHTTPServerTransport behind createMcpExpressApp(), stateless per-request transport+server, Solid session stays a boot-time singleton (keepAlive:true). auth.js .env loading made path-explicit. express declared as direct dep. Error handling maps 401/403/404/409 + the two documented non-throw cases (null without Control, 501 on non-RDF) to actionable MCP error results. GET/DELETE /mcp -> 405, GET /healthz added (no WebID). buildMcpServer(session) factored as a function for 8.3 reuse. mcp-connector/scripts/verify-http.js committed and run live: initialize->tools/list(7 tools)->tools/call solid_list_container against hyperscope_ndb/shared/ — ALL CHECKS PASSED. No secrets in logs (grep-confirmed). README updated. Epic 8 action log appended + progress-report.md Story 8.2 section added, per convention. wacManager.js/podClient.js untouched. Moved to review.
  story-8-3-per-person-endpoints: backlog
  story-8-4-vps-deploy-hardening: backlog
  story-8-5-live-verification: backlog
  story-8-6-team-onboarding-doc: backlog
  epic-8-retrospective: optional

# Sprint Metadata
notes:
  solo_dev_model: "1 human (Nicolas) + Claude Code AI pair programmer. Phases do NOT interleave; one must complete before next begins."
  timebox_guidance: "Phase 1 hard timebox; invest most time in Phase 2 (intellectual core); Phase 3 implements quality data with fallback to 3 journeys; Phase 4 completes with troll run and dashboard."
  context_switching_cost: "Budget 1 ramp-up day per phase transition (different tech stacks)."
  dependencies: "Nginx spike (story-1-2) is critical path with 3-day timebox and explicit fallback plan."

# Known Issues — Privacy Findings (real partial results from Story 2.8)
# These are not artificial failures. They reflect genuine architectural constraints
# discovered during adversarial testing. All are "known unknowns" to address before
# the pilot. Easy fixes should be absorbed into Epics 3-6 stories; hard fixes are
# post-pilot recommendations.
#
# Root cause: pod_resource_uri stored in Qdrant payloads uses the pod slug
# (e.g. "http://localhost:3000/ayoub/"), which encodes the student name.
# Any operator with raw Qdrant access can correlate embeddings to student identities.
#
known_issues:
  - id: PRIV-1
    title: "pod_resource_uri leaks student identity via pod slug"
    source: story-2-8 (vector_privacy.py, 9/14 tests partial)
    severity: medium
    attack_categories_affected: [name-extraction, identity-correlation, location-extraction, grade-extraction]
    status: fixed
    fix_effort: medium
    target: epic-5 (implemented in story-5-2)
    proposed_fix: >
      At embed time, store an opaque UUID (derived from pod_resource_uri via SHA-256)
      in the Qdrant payload instead of the raw URI. Maintain a separate lookup index
      (in Oxigraph or a config file) mapping UUID → pod_resource_uri for authorized
      services only. Changes: embed pipeline (story-2-5 code), traceability module
      (story-2-6 code). Likely absorbed into story-5-2 (deletion cascade) which
      already needs to walk pod→embedding linkage.
    easy_partial_fix: >
      Short-term (no code change): document that Qdrant port 6333 must never be
      exposed outside the Docker network. Already true in current compose config;
      add an explicit note in infra README and story-6-1 report.

  - id: PRIV-2
    title: "Cross-person correlation via shared semantic domain"
    source: story-2-8 (cross-person correlation attack, partial results)
    severity: low
    attack_categories_affected: [cross-person-correlation]
    status: open
    fix_effort: low
    target: epic-6 (document in funder report)
    proposed_fix: >
      This is expected behavior: all students study math, so their embeddings cluster
      by subject domain, not by individual. Not a real leakage risk — an adversary
      learns "these students both study algebra", not any PII. Mitigation is to
      document this clearly in the Epic 6 funder report (story-6-1) as an
      acceptable risk with explanation. No code change needed.

# Ready to implement: call 'bmad-create-story' with story ID from sprint_backlog_order to begin work
