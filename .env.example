# OpenRouter API key (the only external secret)
OPENROUTER_API_KEY=your-key-here

# Volume mount flags for bind mounts: set to ",Z" on Fedora/SELinux, leave empty on Ubuntu
VOLUME_FLAGS=

# CSS base URL — through Nginx for content negotiation support (Spike 1.2: PASS)
# Previously http://localhost:3000 — requires Nginx running (docker-compose up)
CSS_BASE_URL=http://localhost:8080

# Service ports
CSS_PORT=3000        # Internal Docker network port (Nginx → CSS). Do NOT use directly; use CSS_BASE_URL.
OXIGRAPH_PORT=7878
QDRANT_REST_PORT=6333
QDRANT_GRPC_PORT=6334
NGINX_HTTP_PORT=8080
NGINX_HTTPS_PORT=8443

# OpenClaw gateway (GHCR official image)
# use token & password on gateway webpage then `openclaw devices approve` in CLI
OPENCLAW_IMAGE=ghcr.io/openclaw/openclaw:latest
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_GATEWAY_TOKEN=generate-with-openssl-rand-hex-32
GOG_KEYRING_PASSWORD=generate-with-openssl-rand-hex-32
