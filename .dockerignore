# Exclude large directories not needed for Docker build
_bmad/
_bmad-output/
.claude/
.git/
# Exclude most of pipeline/ but keep src/ (needed by acl-manage handler in container)
pipeline/
!pipeline/src/
data/
docs/
